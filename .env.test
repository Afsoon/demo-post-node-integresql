# Loaded by vitest.config.ts with process.loadEnvFile (no dotenv). Committed on purpose: no secrets.
API_TOKEN=test-api-token-0123456789
POLAR_ENVIRONMENT=sandbox
# Fake Polar credentials: the real SDK client runs, each TestApi's MSW server answers instead of Polar
POLAR_ACCESS_TOKEN=polar_oat_test
POLAR_PRODUCT_ID=prod_usage_test
# Reuse only TimescaleDB and its socket volume. pgtest is always fresh.
# CI overrides this to false to remove TimescaleDB; pgtest is retained for inspection.
TESTCONTAINERS_REUSE_ENABLE=true
PGTEST_IMAGE=pgtest-tokio-size-validation:local
# pg pool size per test database (each connection to a fresh clone is a cold backend)
TEST_PG_POOL_MAX=2
# By default, both clients use the TCP host/port of the pgtest test container.
# Optional external pgtest endpoint: skip its container; TimescaleDB setup still runs.
# PGTEST_DATABASE_URL=postgres://metered:metered@localhost:6432/metered
PGTEST_DATABASE_URL=postgres://metered:metered@%2Ftmp%2Fpgtest:6432/metered
